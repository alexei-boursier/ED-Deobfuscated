import * as i from "92597";
__webpack_module__.exports = function (M, v, C) {
  for (var w = t(v), S = k.f, L = b.f, I = 0; I < w.length; I++) {
    var E = w[I];
    if (!i(M, E) && (!C || !i(C, E))) {
      S(M, E, L(v, E));
    }
  }
};