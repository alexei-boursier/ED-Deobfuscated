import * as b from "27674";
__webpack_module__.exports = function (k, M, v) {
  var C;
  var w;
  if (b && i(C = M.constructor) && C !== v && t(w = C.prototype) && w !== v.prototype) {
    b(k, w);
  }
  return k;
};