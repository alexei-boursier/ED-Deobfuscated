var M = RegExp.prototype;
__webpack_module__.exports = function (v) {
  var C = v.flags;
  if (C !== undefined || "flags" in M || t(v, "flags") || !b(M, v)) {
    return C;
  } else {
    return i(k, v);
  }
};